#import "../../../ttao_style.typ": *

// ============================================================
// TTAO Inner Academy — Headmaster's Master Guide
// YEAR TWO · MONTH TWO
// "Putrefaction & the Secret Dissolution"
// Quarter I — Descent into Water
// ============================================================

#month-banner("Two", "Two", "Putrefaction & the Secret Dissolution", "The necessity of decay — Returning the fixed to the volatile")

#v(0.8em)

#hm-note[
  Month 2 deepens the student's relationship with the Nigredo through the lens of
  Water. The focus is on the Raven's Head — the stage of blackness and apparent
  chaos that precedes the emergence of the new. The student must learn to see
  putrefaction not as death, but as the generator of life. Monitor students for
  signs of revulsion or avoidance; the alchemist must be able to sit with the
  "foul smell" of their own inner and outer work.
]

#v(0.5em)

== Month Overview

#table(
  columns: (auto, 1fr, 1fr, 1fr),
  stroke: 0.5pt + ttao-rule,
  fill: (col, row) => if row == 0 { ttao-mid } else if calc.odd(row) { ttao-pale } else { white },
  inset: 0.6em,
  text(fill: white, weight: "bold")[Week],
  text(fill: white, weight: "bold")[Title],
  text(fill: white, weight: "bold")[Laboratory / Fire],
  text(fill: white, weight: "bold")[Inner Work / Psyche],
  [1], [Philosophy of Putrefaction], [Observing natural decay], [The Lesson of the Grave],
  [2], [Controlled Putrefaction], [Fermentation set up], [Sitting with the Raven],
  [3], [Collecting the Volatile], [Collecting fermented spirit], [Breath of the Raven],
  [4], [Inner Fermentation], [Integration of the "seed"], [The Inner Matrice],
)

#v(0.5em)

*Month 2 Deliverables:*
- Active fermentation underway in the laboratory
- Record of color changes and turbidity in lab journal
- Journal entry reflecting on a personal "putrefactive" stage
- Successful collection of a fermented distillate (if timing permits)

#pagebreak()

#include "Y02_M02_W01.typ"
#pagebreak()

#include "Y02_M02_W02.typ"
#pagebreak()

#include "Y02_M02_W03.typ"
#pagebreak()

#include "Y02_M02_W04.typ"
#pagebreak()

// ═══════════════════════════════════════════════════════════
//  MONTH 2 — END OF MONTH CHECKLIST
// ═══════════════════════════════════════════════════════════

= Month 2 — End of Month Checklist

Before the student proceeds to Month 3, confirm all of the following:

#table(
  columns: (auto, 1fr, auto),
  stroke: 0.5pt + ttao-rule,
  fill: (col, row) => if row == 0 { ttao-mid } else if calc.odd(row) { ttao-pale } else { white },
  inset: 0.6em,
  text(fill: white, weight: "bold")[☐],
  text(fill: white, weight: "bold")[Milestone],
  text(fill: white, weight: "bold")[Evidence Form],
  [☐], [Fermentation process understood — can identify sensory markers], [Oral or written check],
  [☐], [Active fermentation recorded — color/scent changes noted], [Lab journal entries shown],
  [☐], [Difference between simple and fermented distillates explained], [Journal entry or quiz],
  [☐], [Inner fermentation journal entry written with sincerity], [Journal entry shown],
)

#v(1em)
#align(center)[
  #line(length: 30%, stroke: 0.8pt + ttao-gold)
  #v(0.3em)
  #text(size: 9pt, fill: ttao-slate, style: "italic")[
    Proceed to Y02\_M03 — The Philosophic Mercury: Working with Water.
  ]
  #v(0.3em)
  #line(length: 30%, stroke: 0.8pt + ttao-gold)
]
